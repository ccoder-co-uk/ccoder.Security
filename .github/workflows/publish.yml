# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json

name: publish

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

env:
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: 1
  DOTNET_NOLOGO: true
  NUGET_DIRECTORY: ${{ github.workspace }}/artifacts/nuget
  TEST_RESULTS_DIRECTORY: ${{ github.workspace }}/artifacts/test-results

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

defaults:
  run:
    shell: pwsh

jobs:
  build-test-pack:
    runs-on: self-hosted
    timeout-minutes: 20
    defaults:
      run:
        shell: powershell

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create version number
        id: version
        run: |
          "value=$(Get-Date -Format 'yyyy.M.d.HHmm')" >> $env:GITHUB_OUTPUT

      - name: Restore
        run: dotnet restore src/cCoder.Security.slnx

      - name: Build
        run: dotnet build src/cCoder.Security.slnx -c Release --no-restore -m /p:BuildInParallel=true /p:UseSharedCompilation=true /p:Version="${{ steps.version.outputs.value }}"

      - name: Test and collect coverage
        run: dotnet test src/cCoder.Security.slnx -c Release --no-build --no-restore -m --settings src/cCoder.Security.runsettings --results-directory "${{ env.TEST_RESULTS_DIRECTORY }}" --logger trx --collect:"XPlat Code Coverage"

      - name: Enforce services and exposures coverage gate
        run: |
          $coverageFiles = Get-ChildItem `
            -Path "${{ env.TEST_RESULTS_DIRECTORY }}" `
            -Recurse `
            -Filter coverage.cobertura.xml

          if ($coverageFiles.Count -eq 0) {
            throw "No Cobertura coverage reports were produced."
          }

          $lines = @{}

          foreach ($coverageFile in $coverageFiles) {
            [xml]$coverage = Get-Content -LiteralPath $coverageFile.FullName

            foreach ($class in $coverage.coverage.packages.package.classes.class) {
              $sourceFile = [string]$class.filename

              if ($sourceFile -notmatch '[/\\](Services|Exposures)[/\\]') {
                continue
              }

              foreach ($line in $class.lines.line) {
                $key = "$sourceFile|$($line.number)"

                if (-not $lines.ContainsKey($key)) {
                  $lines[$key] = 0
                }

                if ([int]$line.hits -gt 0) {
                  $lines[$key] = 1
                }
              }
            }
          }

          if ($lines.Count -eq 0) {
            throw "No service or exposure source lines were found in coverage reports."
          }

          $coveredLines = ($lines.Values | Where-Object { $_ -eq 1 }).Count
          $lineRate = $coveredLines / $lines.Count
          $percentage = [Math]::Round($lineRate * 100, 3)
          Write-Host "Services and exposures line coverage: $percentage% ($coveredLines/$($lines.Count))"

          if ($lineRate -le 0.95) {
            throw "Services and exposures line coverage must be greater than 95%; actual: $percentage%."
          }

      - name: Upload test and coverage results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          if-no-files-found: error
          retention-days: 7
          path: ${{ env.TEST_RESULTS_DIRECTORY }}

      - name: Pack
        run: |
          New-Item -ItemType Directory -Path "${{ env.NUGET_DIRECTORY }}" -Force | Out-Null
          dotnet pack src/cCoder.Security.slnx -c Release --no-build --no-restore -m --output "${{ env.NUGET_DIRECTORY }}" /p:BuildInParallel=true /p:Version="${{ steps.version.outputs.value }}"

      - name: Upload package artifacts
        uses: actions/upload-artifact@v4
        with:
          name: nuget
          compression-level: 0
          if-no-files-found: error
          retention-days: 7
          path: |
            ${{ env.NUGET_DIRECTORY }}/*.nupkg
            !${{ env.NUGET_DIRECTORY }}/*.snupkg

  publish:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs: build-test-pack
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Download package artifacts
        uses: actions/download-artifact@v4
        with:
          name: nuget
          path: ${{ env.NUGET_DIRECTORY }}

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.x

      - name: NuGet login
        id: login
        uses: NuGet/login@v1
        with:
          user: ${{ secrets.NUGET_USER }}

      - name: Publish packages
        run: |
          foreach ($file in (Get-ChildItem "${{ env.NUGET_DIRECTORY }}" -Filter *.nupkg)) {
            dotnet nuget push $file.FullName --api-key "${{ steps.login.outputs.NUGET_API_KEY }}" --source https://api.nuget.org/v3/index.json --skip-duplicate
          }

  coverage-report:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs: build-test-pack
    timeout-minutes: 10
    permissions:
      contents: read
      id-token: write
      pages: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    defaults:
      run:
        shell: bash

    steps:
      - name: Download test and coverage results
        uses: actions/download-artifact@v4
        with:
          name: test-results
          path: test-results

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.x

      - name: Generate coverage report
        run: |
          dotnet tool install --tool-path tools dotnet-reportgenerator-globaltool --version 5.5.10
          tools/reportgenerator \
            -reports:"test-results/**/coverage.cobertura.xml" \
            -targetdir:"coverage-report" \
            -reporttypes:"Html;Cobertura;MarkdownSummaryGithub"
          cat coverage-report/SummaryGithub.md >> "$GITHUB_STEP_SUMMARY"
          touch coverage-report/.nojekyll

      - name: Upload coverage report artifact
        uses: actions/upload-artifact@v4
        with:
          name: code-coverage-report
          if-no-files-found: error
          retention-days: 30
          path: coverage-report

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: coverage-report

      - name: Deploy coverage report to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
